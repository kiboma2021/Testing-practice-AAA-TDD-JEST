class newString {
  stringLength(string) {
    let strlength=string.length;
    if (strlength > 0 & strlength <= 10) {
      return strlength;
    } else {
      return strlength;
    }
  }
}

module.exports = newString;